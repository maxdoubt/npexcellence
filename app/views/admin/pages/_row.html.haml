%li{id: 'list_' + page.id.to_s}
  %div
    = link_to page.title, admin_page_path(page)
    - if page.published
      (Published)

    .pull-right
      = link_to 'Edit', edit_admin_page_path(page), class: 'btn btn-default'
      = link_to 'Delete', admin_page_path(page), class: 'btn btn-danger', method: 'delete', data: { confirm: "Are you sure?" }

  - puts page.children.inspect
  - page.children.each do |page|
    %ol
      = render 'row', page: page