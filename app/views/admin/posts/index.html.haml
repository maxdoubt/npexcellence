%h1 Posts

%p= link_to 'New Post', new_admin_post_path, class: 'btn btn-primary'

%table.table.table-default.table-striped.table-bordered
  %tr
    %th Title
    %th User
    %th Body
    %th Published
    %th Created At
    %th Actions

  - @posts.each do |post|
    %tr
      %td= link_to post.title, admin_post_path(post)
      %td= post.user.email
      %td= post.body.split(' ')[0..30].join(' ')
      %td= post.published
      %td= post.created_at
      %td
        = link_to 'Destroy', admin_post_path(post), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger'
        = link_to 'Edit', edit_admin_post_path(post), class: 'btn btn-default'