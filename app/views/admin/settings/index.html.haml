%h1 Settings

%p= link_to 'Edit Settings', edit_admin_setting_path(@setting), class: 'btn btn-primary'

%fieldset.well
  .row
    .col-md-12
      .form-group
        %label Footer
        = @setting.footer
