%h1 Uploads

%p= link_to 'New Upload', new_upload_path, class: 'btn btn-primary'

%table.table.table-default.table-striped.table-bordered
  %tr
    %th Filename
    %th Size
    %th Content Type
    %th Thumbnail
    %td Updated At
    %td Actions

  - @uploads.each do |upload|
    %tr
      %td= upload.upload_file_name
      %td= upload.upload_file_size
      %td= upload.upload_content_type
      %td
      %td= upload.upload_updated_at
      %td
        = link_to 'Destroy', upload_path(upload), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger'
        = link_to upload.upload.url, class: 'btn btn-default' do
          Download
          %i.fa.fa-download